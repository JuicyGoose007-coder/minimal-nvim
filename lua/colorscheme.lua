-- vim.cmd.colorscheme("gruvbox")

-- "original" over the default "material": its fg is #ebdbb2 and its accents are
-- the classic gruvbox brights, which is what Noctalia writes into the terminal
-- and tmux palettes. Backgrounds stay material's softer ones.
vim.g.gruvbox_material_foreground = "original"
vim.cmd.colorscheme("gruvbox-material")
